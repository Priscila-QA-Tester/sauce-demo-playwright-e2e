# qa-playwright-bdd-framework

Minimal Playwright demo project for learning QA testing.

## Usage

- Run `npm install` once to install dependencies.
- Run `npm test` to execute the sample demo test.

## Files

- `tests/demo.spec.js` — example browser test
